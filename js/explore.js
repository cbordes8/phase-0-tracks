// To write a string reverse function I need to fisrt be able to split the string
// Then the string will be set in a array however in the case of this challenge the letters cant be serparte
// So I remove or take away the spaces to combine the words together

function reverse(string) {
  return string.split('').reverse().join('');
}
reverse("palindrome")

Boolean(1 == 1)
